/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",

  assetPrefix: "https://webdev.gitam.edu/next-testing/",
  trailingSlash: true,
  basePath: "",
};

export default nextConfig;
